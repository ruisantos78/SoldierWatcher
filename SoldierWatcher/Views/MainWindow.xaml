<Window x:Class="SoldierWatcher.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SoldierWatcher.Views" 
        xmlns:viewmodels="clr-namespace:SoldierWatcher.ViewModels"         
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=viewmodels:MainViewModel}"
        Title="SoldierWatcher" Height="450" Width="800">
    <DockPanel>
        <Border DockPanel.Dock="Top">
            
        </Border>

        <Menu DockPanel.Dock="Top">
            
        </Menu>

        <StatusBar DockPanel.Dock="Bottom">
            
        </StatusBar>

        <Frame Source="{Binding CurrentPage}">
            
        </Frame>
    </DockPanel>
</Window>
